import { sharedUtilityHelpers } from './SharedUtilityHelpers';

describe('sharedUtilityHelpers', () => {
  it('should work', () => {
    expect(sharedUtilityHelpers()).toEqual('shared-utility-helpers');
  });
});
